<%= render 'navbar' %>
<div class="container mt-4">
  <div class="row">
    <%  @articles.each do |article| %>
      <h2 class="title"> <%= link_to article.title, article %></h2>
      <p><%= article.content %></p>
    <% end %>
  </div>
</div>
